'use client';

import { Button } from '@/app/_components/ui/button';

const Header = () => {
  return (
    <div className="fixed right-0 top-0 p-2 flex">
      <Button>Voter Resources</Button>
    </div>
  );
};

export default Header;
